[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.1.1",
    "flask-socketio>=5.5.1",
    "pydub>=0.25.1",
    "mblt-model-zoo[transformers,MeloTTS]>=1.5.1",
]

[tool.black]
line-length = 120
